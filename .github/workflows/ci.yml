name: MERN CI  # 👈 Workflow name shown in GitHub Actions tab

on:
  push:
    branches: [main]  # 👈 Triggers workflow whenever code is pushed to 'main' branch

jobs:
  mongo-init-test:  # 👈 Defines a single job named 'mongo-init-test'
    runs-on: ubuntu-latest  # 👈 Uses latest Ubuntu runner as virtual environment
    
    env:
      PORT: ${{ secrets.PORT }}  # 👈 Loads environment variables securely from GitHub Secrets
      MONGO_URI: ${{ secrets.MONGO_URI }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}

    services:
      mongo:  # 👈 Declares a MongoDB service container
        image: mongo  # 👈 Uses official MongoDB Docker image
        ports: ['27017:27017']  # 👈 Exposes MongoDB port for local access inside the job
    
    steps:
      - name: Checkout Repository  # 👈 Step 1: Pulls your repo code into the runner
        uses: actions/checkout@v5  # 👈 Official GitHub Action for checking out code

      - name: Setup Node.js  # 👈 Step 2: Prepares Node.js environment
        uses: actions/setup-node@v3  # 👈 Official Node setup action
        with: 
          node-version: '18'  # 👈 Specifies Node.js version to use

      - name: Waiting for MongoDB to be ready  # 👈 Step 3: Ensures MongoDB container is fully running before use
        run: |  # 👇 Simple loop to check MongoDB readiness using netcat (nc)
          until nc -z localhost 27017; do
            echo "Waiting for MongoDB..."
            sleep 2
          done
          echo "MongoDB is up"

      - name: Seed MongoDB  # 👈 Step 4: Prepares test data in MongoDB
        working-directory: ./backend  # 👈 Changes directory to backend folder
        run: |  # 👈 Installs backend dependencies and Runs custom seeding script to populate DB
          npm install
          node seedTasksData.js

      - name: Running Backend Tests  # 👈 Step 5: Executes automated backend tests
        working-directory: ./backend
        run: npm test  # 👈 Runs test scripts defined in package.json
